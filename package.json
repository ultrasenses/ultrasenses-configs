{
  "name": "ultrasenses-utils",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "private": true,
  "description": "Ultrasenses utils",
  "author": "Ultrasenses <contact.ultrasenses@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/ultrasenses/ultrasenses-utils.git"
  },
  "scripts": {
    "build": "yarn run lerna:build",
    "lerna:build": "lerna run build",
    "lerna:bootstrap": "lerna bootstrap",
    "lerna:publish": "lerna bootsrap && lerna run build && lerna publish from-package --conventional-commits --yes",
    "eslint:check": "eslint . --ignore-path .gitignore",
    "update:version": "lerna version --conventional-commits --no-push"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "eslint": "^7.20.0"
  },
  "workspaces": [
    "packages/**/*"
  ]
}
